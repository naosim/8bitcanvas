{
  "name": "tinytidycanvas",
  "version": "1.0.0",
  "description": "Canvas-based node editor",
  "scripts": {
    "build": "bash build.sh",
    "watch": "tsc --watch",
    "desktop:run": "neu run",
    "desktop:build": "neu build",
    "desktop:build:release": "neu build --release"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  },
  "optionalDependencies": {
    "@esbuild/linux-x64": "^0.28.0",
    "@esbuild/win32-x64": "^0.28.0"
  },
  "dependencies": {
    "@neutralinojs/lib": "^6.7.0"
  }
}
